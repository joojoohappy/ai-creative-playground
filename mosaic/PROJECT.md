# Project

Establish a shared directory structure before parallel Fable development begins. This scaffold contains placeholders only; application setup, UI, generation logic, and contract definitions remain to be implemented.

<!-- Ownership:
User: app/page.tsx, app/recipe/[id]/page.tsx, app/create/[id]/page.tsx,
app/result/[id]/page.tsx, components/gallery/, components/recipe/,
components/create/, services/generation-client.ts, data/recipes.ts, public/seed/.
Teammate: app/api/generate/route.ts, services/generation-server.ts, lib/ai/.
Shared: components/shared/.
Shared contracts: types/recipe.ts, types/generation.ts.
Coordinate shared components and contract changes before parallel implementation.
-->
